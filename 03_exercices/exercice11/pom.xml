<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.11</version>
        <relativePath/>
    </parent>

    <groupId>com.example</groupId>
    <artifactId>exercice11</artifactId>
    <version>1.0.0</version>
    <name>exercice11</name>
    <description>API Spring Boot de gestion de tickets de support avec TDD et BDD</description>

    <properties>
        <java.version>21</java.version>
        <cucumber.version>7.20.1</cucumber.version>
        <jacoco.version>0.8.14</jacoco.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>io.cucumber</groupId>
            <artifactId>cucumber-junit-platform-engine</artifactId>
            <version>${cucumber.version}</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>io.cucumber</groupId>
            <artifactId>cucumber-java</artifactId>
            <version>${cucumber.version}</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>io.cucumber</groupId>
            <artifactId>cucumber-spring</artifactId>
            <version>${cucumber.version}</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.junit.platform</groupId>
            <artifactId>junit-platform-suite</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <useModulePath>false</useModulePath>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>${jacoco.version}</version>
                <configuration>
                    <excludes>
                        <exclude>com/example/supportticket/SupportTicketApplication.class</exclude>
                        <exclude>com/example/supportticket/model/*.class</exclude>
                        <exclude>com/example/supportticket/exception/*.class</exclude>
                        <exclude>com/example/supportticket/bdd/CucumberSpringConfiguration.class</exclude>
                        <exclude>com/example/supportticket/bdd/RunCucumberTest.class</exclude>
                    </excludes>
                </configuration>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <!-- Copie les rapports de test dans resultat-test/ après mvn verify -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-antrun-plugin</artifactId>
                <version>3.1.0</version>
                <executions>
                    <execution>
                        <id>export-test-results</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <target>
                                <mkdir dir="${project.basedir}/resultat-test"/>
                                <mkdir dir="${project.basedir}/resultat-test/surefire-reports"/>
                                <mkdir dir="${project.basedir}/resultat-test/jacoco"/>
                                <mkdir dir="${project.basedir}/resultat-test/cucumber"/>

                                <copy todir="${project.basedir}/resultat-test/surefire-reports" failonerror="false" overwrite="true">
                                    <fileset dir="${project.build.directory}/surefire-reports"/>
                                </copy>

                                <copy file="${project.build.directory}/cucumber-report.html"
                                      tofile="${project.basedir}/resultat-test/cucumber/cucumber-report.html"
                                      failonerror="false" overwrite="true"/>
                                <copy file="${project.build.directory}/cucumber-report.json"
                                      tofile="${project.basedir}/resultat-test/cucumber/cucumber-report.json"
                                      failonerror="false" overwrite="true"/>

                                <copy todir="${project.basedir}/resultat-test/jacoco" failonerror="false" overwrite="true">
                                    <fileset dir="${project.build.directory}/site/jacoco"/>
                                </copy>
                            </target>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
